// The only reason of this code is to test that compiling fails
// if the compiler flags are wrong. A preprocessir directive in the
// irtualFunctionTemplate raises a compiler error in this case, and
// we are testing if this works as intended.

#include "VirtualFunctionTemplate.h"
